# Ollive Inference Logger

A lightweight chatbot + inference logging/ingestion system.

- `backend/` — Express API: chat endpoint (Groq-backed), logging SDK, ingestion pipeline, Postgres storage
- `frontend/` — React (Vite) chat UI

## Status

Scaffolding in progress.
